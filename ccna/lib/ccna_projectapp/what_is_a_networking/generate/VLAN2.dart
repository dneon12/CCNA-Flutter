import 'package:flutter/material.dart';


class GeneratedVLAN2Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      '''VLANs work by applying tags to network frames and handling these tags in networking system–creating the appearance and functionality of network traffic that is physically on a single network but acts as if it is split between separate networks.
In this way, VLANs can keep network applications separate despite being connected to the same physical network and without requiring multiple sets of cabling and networking devices to be deployed.''',
      overflow: TextOverflow.visible,
      textAlign: TextAlign.left,
      style: TextStyle(
        height: 1.171875,
        fontSize: 12.0,
        fontFamily: 'Noto Serif',
        fontWeight: FontWeight.w400,
        color: Color.fromARGB(255, 0, 0, 0),

        /* letterSpacing: 0.0, */
      ),
    );
  }
}
